<UserControl x:Class="HVACrate2.App.Controls.CompassControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="110" Height="110">
    <Border CornerRadius="55" Background="White" BorderBrush="{StaticResource AccentBrush}" BorderThickness="2">
        <Grid x:Name="RotatingGrid" RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <RotateTransform x:Name="Rotation" Angle="0" />
            </Grid.RenderTransform>

            <!-- Vector fallback rose, shown until Assets/compass.png is available -->
            <Canvas x:Name="FallbackRose" Width="106" Height="106">
                <Polygon Points="53,8 63,53 53,45 43,53" Fill="{StaticResource AccentBrush}" />
                <Polygon Points="53,98 63,53 53,61 43,53" Fill="#C9C9C9" />
                <TextBlock Text="N" Canvas.Left="47" Canvas.Top="10" FontWeight="Bold" FontSize="11" Foreground="{StaticResource TextPrimaryBrush}" />
            </Canvas>

            <Image x:Name="CompassImage" Stretch="Uniform" Visibility="Collapsed" />
        </Grid>
    </Border>
</UserControl>
