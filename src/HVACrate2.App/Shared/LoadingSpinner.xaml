<UserControl x:Class="HVACrate2.App.Shared.LoadingSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="28" Height="28">
    <UserControl.Resources>
        <Storyboard x:Key="SpinStoryboard">
            <DoubleAnimation Storyboard.TargetName="Rotation" Storyboard.TargetProperty="Angle"
                              From="0" To="360" Duration="0:0:0.9" RepeatBehavior="Forever" />
        </Storyboard>
    </UserControl.Resources>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource SpinStoryboard}" />
        </EventTrigger>
    </UserControl.Triggers>
    <Grid Width="28" Height="28">
        <Ellipse Width="28" Height="28" StrokeThickness="3" Stroke="{DynamicResource CardBorderBrush}" />
        <Path Data="M14,1 A13,13 0 1 1 1,14" Stroke="{DynamicResource AccentBrush}" StrokeThickness="3"
              StrokeStartLineCap="Round" StrokeEndLineCap="Round" RenderTransformOrigin="0.5,0.5">
            <Path.RenderTransform>
                <RotateTransform x:Name="Rotation" Angle="0" />
            </Path.RenderTransform>
        </Path>
    </Grid>
</UserControl>
